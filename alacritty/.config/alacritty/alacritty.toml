[window]
dimensions = { columns = 100, lines = 32 }
padding = { x = 1, y = 0 }

# Does not work on Wayland
position = { x = 35, y = 65 }

# -- Does not work on X11 ("opacity" does actually work but I usually pair it with "blur") --
#opacity = 0.50
#blur = true

[font]
size = 11

[cursor]
style = { shape = "Beam", blinking = "On" }
